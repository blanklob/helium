root: .

# Disabling some checks
RequiredDirectories:
  enabled: false

# Must be enabled if you care about translations
MatchingTranslations:
  enabled: false

# Ignoring some files
ignore:
  - .config
  - tsconfig.json
  - package.json
