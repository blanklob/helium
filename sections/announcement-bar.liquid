<div class="s-announcement-bar u-pv-sm" data-section-id="{{ section.id }}">
  <div class="s-announcement-bar_wrapper u-container">
    <p class="s-announcement-bar_message  u-b3" aria-label="Announcement message" tabindex="0">{{ section.settings.message | escape }}</p>
  </div>
</div>

{%- style -%}
.s-announcement-bar {
  background-color: {{- section.settings.bg_color -}};
  color: {{- section.settings.text_color -}};
}
{%- endstyle -%}

{% schema %}
{
  "name": "Announcement bar",
  "settings": [
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "message",
      "label": "Text",
      "default": "Free shipping on orders over 100$."
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Bar",
      "default": "#111111"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text",
      "default": "#ffffff"
    }
  ]
}
{% endschema %}
